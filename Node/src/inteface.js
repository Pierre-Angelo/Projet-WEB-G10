import "./styles.css";


const Interface = (DATA) => {
    let DB = DATA.DATA;
    return (
    <div className="App">
        Test
        <div>
        <button onClick={() => this.addThemeToServer("test")}>addTh</button>
        <button onClick={() => this.rmThemeFromServer("test")}>rmTh</button>
        </div>
        <div>
        <button onClick={() => this.addFlashcardsToServer("test", "test1", "test2")}>addCard</button>
        <button onClick={() => this.rmFlashcardsFromServer("test", 0)}>rmCard</button>
        </div>
        <div>Voici une flashcard DB[0].name</div>
    </div>)}

export default Interface